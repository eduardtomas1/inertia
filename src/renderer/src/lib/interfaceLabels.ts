/**
 * Small, stable interface vocabulary that can move behind a translator later
 * without coupling product copy to component structure.
 */
export const COMPOSER_LABELS = Object.freeze({
  skills: "Skills",
  scratchPrompts: "Scratch prompts",
  savedScratchPrompts: "Saved scratch prompts",
  saveScratchPrompt: "Save current prompt",
  noScratchPrompts: "No scratch prompts saved yet.",
});
