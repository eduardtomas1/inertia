# Combined review evidence — next release

Work in progress. Baseline: `5c9dc929b2ecfcd228b4a013fa770c0683e6108f` (fresh origin/main, merged #357). Version remains 0.0.55. Both full issue bodies and all 13 comments were fetched on 2026-09-13. The 177 IDs below are external claims, not 177 established defects. Pending rows are explicitly unaudited; this document does not claim completion.

Source pointers identify functions when line positions are changing. Reproductions use synthetic data and isolated fixtures. Hosted/native validation and final current-head CI are still pending.

## Issue #356

| Finding | Disposition | Evidence and limits |
| --- | --- | --- |
| [X1](https://github.com/eduardtomas1/inertia/issues/356) — [High · Bug/Tests] `main` is red locally: locale-dependent number formatting in an English-only UI | already fixed | Current baseline assertions already derive expected numbers using the same locale as the UI (chat-goal-control.dom.test.tsx:349; private-connect-workspace.dom.test.tsx:166). Independent LANG=ca_ES.UTF-8 LC_ALL=ca_ES.UTF-8 focused run passes all 19 tests. Product still intentionally uses OS-localized numeric/date formatting; English prose alone does not establish that localized group separators are incorrect. No global locale override added. |
| [X2](https://github.com/eduardtomas1/inertia/issues/356) — [High · Structure] The 1,250-line ceiling is being met by squeezing code, not by splitting modules | independently confirmed | Current source still contains compressed declarations/effects near architectural line ceilings. This is a maintainability constraint, not independent proof of a runtime failure. This PR extracts run-state validation, checkpoint recovery, permission detail and review-note UI at concrete responsibility boundaries; repository-wide reformatting/count-based splits are deferred. |
| [X3](https://github.com/eduardtomas1/inertia/issues/356) — [Medium · Structure] Migrations import live runtime modules, and the lineage pin forces type lies, runtime mutation and function forks | independently confirmed | Directly encountered: changing live attachment MIME lookup broke the released migration-56 source pin during the full gate. Restored the pinned function exactly and added a safe live lookup. Released migrations/dependency declarations cannot be rewritten; future migrations should use frozen historical dependencies. Existing model/provider pinned compatibility declarations remain a structural debt. |
| [S1](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Critical · Bug · Data loss] `checkpoint.revert` restores the entire worktree with no backup: it deletes files staged after the checkpoint and discards unrelated edits | independently confirmed | `src/server/checkpoints.ts:restoreCheckpoint` restores the whole worktree. `tests/server/checkpoints.test.ts` proves later unstaged and staged-file worktree bytes disappear, the real index survives, and the new durable pre-restore checkpoint recovers the exact later bytes. Persistence failure and ignored-file overlap block mutation. Whole-project scope remains explicit. Concurrent external edits during the Git operation are not a transactional filesystem snapshot. |
| [S2](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [High · Bug] A synchronous throw from `providers.run()` wedges the turn in "cancelling" until the runtime restarts | already fixed | Baseline ProviderRunCoordinator records an exact no-spawn cleanup receipt when capability admission refuses before harness launch. Real coordinator regression provider-run-admission-cleanup proves this path; cleanup-proof tests also preserve authority for truly missing/identity-mismatched and post-start throws. Do not blindly clear providerRunStarted on arbitrary synchronous exceptions. |
| [S3](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [High · Bug] Exceptions in the terminal-result handler are swallowed, so the turn shows "running" for up to 6 h after the provider exits | already fixed | Merged #357 correction remains in turn-controller.ts: provider-result persistence failure requests tracked cancellation before settlement; ownership is retained until exact cleanup. Both result-persistence and cancellation-persistence suites passed in the 25-test merged-regression batch on this branch. |
| [S4](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [High · Bug] Shutdown skips every resource-owning cleanup phase if a long command is in flight | independently confirmed | Named isolated-review/compaction deadlock was repaired in the baseline by lifetime cancellation before quiescence; isolated cancellation tests pass for settled and force-detached outcomes. The generic outer shutdown catch still skips subsequent phases after an arbitrary quiesce timeout; that broader failure mode remains unresolved and must retain database/ownership safety. |
| [S5](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [High · Bug] Terminal output backpressure kills the entire runtime WebSocket at 1 MB, while runtime events explicitly allow 64 MB | already fixed | terminal-socket.ts delegates to sendRuntimeEvent and preserves immediate admission feedback. tests/server/terminal-socket.test.ts passed in the 25-test merged-regression batch; shared transport cap/stall/failed-write controls remain asserted. |
| [S6](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Performance] Every provider event, including each streamed text chunk, costs 3 SQLite row reads and re-arms 2 timers | independently confirmed | Inspected turn-timeout-coordinator.activity: it asks the injected status callback twice and rearms observation/inactivity timers per activity; turn-controller also persists/validates ownership identity. This establishes repeated work, not a measured user-visible slowdown. Replacing durable identity checks with in-memory assumptions is not justified by the metric alone. Timer coalescing remains unimplemented pending behavioral/latency evidence. |
| [S7](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Performance] Every broadcast frame is JSON-serialised 1 + N times, and a full snapshot is serialised once per client | independently confirmed | RuntimeSequencer.retain serializes to measure bytes; RuntimeSyncHub.broadcastCommitted projects and sends per subscription. Authority-dependent projection means a serialized main frame cannot be reused blindly for detached clients. A cache keyed by projected immutable frame could reduce repeats, but latency/heap impact was not measured and no authority-boundary rewrite was introduced. |
| [S8](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Performance] `currentSnapshot()` / `shellSnapshot()` are used to answer yes/no questions | independently confirmed | Confirmed full currentSnapshot calls for stopRun lookup, update agentWorkActive and a discarded initialization call in index.ts; detached-chat-runtime-policy also uses a snapshot for ownership checks. Targeted-query alternatives need equivalent safety-lock/diagnostic and authority semantics. Snapshot cost and latency threshold were not measured; these paths remain unchanged. |
| [S9](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Security] Raw internal error messages, including absolute user paths, reach the renderer from document preparation | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [S10](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Bug] `message.send` overwrites a user rename via a stale title check, and can split surrogate pairs | independently confirmed | The title check in turn-interaction-commands.ts now rereads current state immediately before writing and truncates a bounded prefix by Unicode code point. tests/server/turn-interaction-commands.test.ts passes 55 tests including a rename during asynchronous attachment preparation and a surrogate-pair boundary. |
| [S11](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Bug] The read-only `state()` path deletes the durable native goal without the goal lock or a tombstone | independently confirmed | agent-workflow-controller.state filtered a stale native goal and synchronously called clearAgentGoal without session/tombstone. State now only filters; native refresh retains its existing locked, session-bound/timestamped clear. Direct read regression preserves the durable stale record. Controller/command/persistence suites pass 54 tests; the hypothesized first-start race was not independently reproduced. |
| [S12](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Medium · Performance] Synchronous level-9 gzip/gunzip of patches up to 2 MiB on the runtime event loop | independently confirmed | turn-git-artifacts.ts compressed/decompressed bounded patches synchronously on the runtime thread. It now uses promisified zlib operations with the same level-9 representation, byte/output caps and digest verification. Git artifact/runner/transaction suites pass 80 tests, including shutdown settlement. No claimed numerical latency improvement or compression-format change. |
| [S13](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Low · Bug/Performance] Duo `cancellationRequests` leaks an entry on every post-prepare cancel and every unknown-launch cancel | independently confirmed | duo-launch-coordinator.ts added every cancelled ID to a Set but removed IDs only in prepareFresh finally. Markers are now admitted only while that preparation task exists; durable cancellation stays in the store. Focused tests pass for preparation-race cancellation and zero retained markers after durable/missing launches. |
| [S14](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Low · Quality] The Private Connect admission gate is copied 3× in `index.ts`, plus again in `dispatchCommand` | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [S15](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804750) — [Low · Structure] `startRuntime` is a ~1,100-line closure, and `createProcessReplacing` takes 15 positional parameters | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [D1](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Critical · Bug · Data loss] Startup recovery treats FK orphans, schema-text drift and *any* exception as "corrupt", swaps in a backup, and **deletes every backup that fails the same check** | independently confirmed | Baseline now throws on non-corruption validation exceptions, validates backups before quarantining the primary, and no longer deletes invalid backups during recovery. Remaining schema/FK checks still return corrupt and can trigger automatic restoration or an empty database with preserved primary evidence. This remaining logical-inconsistency recovery policy is unresolved; do not describe the entire finding as fixed. |
| [D2](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [High · Performance] Every turn settlement full-scans `messages` and `agent_reasonings` inside the write transaction, because no index starts with `turn_id` | independently confirmed | `src/server/persistence/stream-text-storage.ts` filtered compaction by turn alone. The two settlement functions and assistant-snapshot cleanup now include the owning conversation, using existing indexes without changing released migrations. Actual SQLite query-plan and data-integrity tests are in `tests/server/stream-text-storage.test.ts`; no user latency threshold or multi-GB profile was measured. |
| [D3](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Medium · Bug] A failed `git worktree add -b` leaks the new branch and records the attempt as "not-created" | already fixed | Baseline createWorktreeWithOwnershipReceipt already calls inspectUnacknowledgedWorktreeCreation and calls notAdded only for absent state. Existing real-Git permission regression retains attribution for a branch created before worktree failure and verifies the branch remains discoverable. Windows uses a different permission model; that Unix regression intentionally skips there. |
| [D4](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Medium · Bug] `git worktree list --porcelain -z` needs Git ≥ 2.36, and there is no version check or fallback | independently confirmed | registeredWorktrees unconditionally uses porcelain -z. Git 2.36 release notes confirm that option was introduced to avoid unsafe pathname quoting (https://raw.githubusercontent.com/git/git/v2.36.0/Documentation/RelNotes/2.36.0.txt). No official older minimum was found in repository docs. Native Git 2.34 behavior was not run; a newline fallback would need hostile-path correctness proof and is not introduced. Minimum-version admission remains open. |
| [D5](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Medium · Performance] SQL is recompiled on every call, including the streaming hot path | independently confirmed | RuntimeStore requireProject/requireConversation/requireAgentTurn and stream-text-storage create statements on each call. The installed better-sqlite3 prepare wrapper constructs a statement each call. No preparation-time/heap profile demonstrates this dominates latency; database-lifetime caches and transaction wrapper caching would require schema-change/lifetime handling, so no blanket cache was introduced. |
| [D6](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Medium · Performance] Every launch blocks on a full `quick_check` plus a whole-DB `foreign_key_check` before the database opens | deliberate tradeoff | validateOpenDatabase and startup recovery perform integrity/schema/FK validation before trusting existing data or a backup. This is deliberate corruption/ownership protection required by AGENTS.md. An unclean-shutdown sentinel cannot prove a database was not externally modified after clean shutdown. No multi-GB startup latency witness was run; validation was not weakened. |
| [D7](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Medium · Structure/Performance] `git.diff` and `git.workspace.diff` are ~80-line copy-pastes that have already drifted, and each diff costs about 10 git spawns just for identity checks | independently confirmed | Confirmed git.diff discards reconcileReviews return and broadcasts for every complete conversation-wide diff, whereas git.workspace.diff gates its broadcast. Extra identity checks deliberately bracket asynchronous operations; merging them into one initial check would weaken at-use ownership validation. Duplicate broadcast cost remains unmeasured and unchanged. |
| [D8](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Medium · Quality] The Duo "blocking" predicate and status lists are copy-pasted and already differ; guarded transitions fail silently | not reproduced | Inspected paired-launch comparison CAS updates: they return the actual stored row, including its unchanged state when an update loses a race, rather than a success boolean. attachComparisonTurn has a distinct first-adoption requirement. Repeated status SQL is real structural duplication; no user-visible lost-race failure was reproduced, and released trigger definitions were not rewritten for stylistic uniformity. |
| [D9](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Low · Performance] Deleting a conversation or project sorts and JSON-parses every message's attachments in the database | independently confirmed | transcript-repository.attachments reads ordered attachment JSON rows and applies the strict persisted codec. A targeted SQL membership replacement must preserve malformed-record rejection and lifetime ownership; JSON ID membership alone is not equivalent. Source establishes full-history work, but no latency threshold was measured. No schema or retention-semantics change made. |
| [D10](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Low · Structure] `RuntimeStore` is a ~150-method pass-through facade squeezed under the cap | deliberate tradeoff | RuntimeStore exposes some repositories and wraps others, retains an unused compatibility constructor argument, and stores issue-report SQL directly. This is verified organizational debt, not a reproduced incorrect result. Broad facade/API changes and released migration extraction were not needed for the targeted indexed compaction fix. |
| [D11](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Low · Performance/Bug risk] The legacy turn-backfill migration is quadratic and spreads unbounded arrays into `Math.min`/`Math.max` | externally blocked | Released migration runner spreads eventTimes into min/max. A bounded Node 22 probe with 150,000 timestamps produced RangeError, confirming the operation limit; a complete legacy-database upgrade witness was not built. Repairing the already-released backfill requires a migration-maintenance decision consistent with the explicit append-only invariant. The pinned migration was not rewritten and no user database was modified. |
| [D12](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Low · Quality] Dead or misleading worktree APIs, including an unused `--force` removal | deliberate tradeoff | removeOwnedWorktree explicitly retains registered checkouts because Git cannot atomically bind destructive removal to the inspected identity. Its repeated inspections are exercised by mutation-race tests. The general removeWorktree helper has only test consumers and no production command/IPC caller. Existence of an explicitly invoked force option in fixtures does not establish a live destructive path; no blanket removal was enabled. |
| [D13](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Low · Structure] The ref transaction re-implements the git process lifecycle, and the two copies have already drifted | independently confirmed | Prepared ref transactions lacked the git ownership label and re-concatenated stderr to measure retained length. Added the same git label and an incremental bounded byte counter. Both transaction and ordinary runner lifecycle paths retain their existing timeout/complete-tree cleanup logic; 80 focused Git/artifact tests pass. A general child-runner rewrite was not needed. |
| [D14](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804863) — [Low · Structure] The backup-validator worker is built from `Function.prototype.toString()` + `eval`, and runs on a different SQLite engine | not reproduced | validateDatabaseOffThread builds a checked-in function source worker using node:sqlite while startup uses better-sqlite3. This is a maintenance/engine divergence risk; the full unit run passed backup/recovery coverage and no present transformed-helper failure was reproduced. The claim that every scheduled backup currently fails is not established. A dedicated bundled worker remains a structural alternative. |
| [P1](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [High · Bug] Cursor skips process-tree termination when chat-tool (MCP) cleanup fails | already fixed | Cursor now records host-tool close failure, always joins terminateOwnedProcessTree, and combines failure results with structured cleanup diagnostics. Independent cursor-acp-cleanup-join focused cases pass with both confirmed and unconfirmed process termination. Three cleanup suites pass 11 tests. |
| [P2](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [High · Structure] Three ACP harnesses are 42–68% copy-paste. This is the root cause of P1 and P3–P6 | independently confirmed | Adapter duplication is present and P3-P6/P9 behavioral drift was reproduced and corrected with shared permission detail/redaction and cross-adapter framing cases. A wholesale shared ACP engine is not implemented: auth, session evidence and extension behavior differ, and replacing all lifecycle ownership at once needs equivalent conformance coverage. |
| [P3](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Medium · Security] Cursor puts raw, unsanitised provider error text (up to 1 MiB) into the user-visible `failure.message` | independently confirmed | `src/server/provider/cursor-acp-failures.ts:cursorRuntimeFailure` passed raw message through. It now bounds and sanitizes public summaries and technical detail. `tests/server/acp-permission-hardening.test.ts` covers synthetic secret/path removal. No authenticated Cursor service was used. |
| [P4](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Medium · Security] Cursor and Kimi stderr skip the environment-secret redaction Gemini has, and decode UTF-8 per chunk | independently confirmed | Cursor and Kimi stderr decoded Buffer chunks individually. Both now use stream UTF-8 decoding and the existing exact environment-secret redactor. The in-memory ACP test splits Japanese text and a synthetic environment value across chunks and asserts preserved text, redaction and cleanup. This does not claim Cursor/Kimi dotenv discovery matches Gemini. |
| [P5](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Medium · Security] Cursor and Kimi approval prompts show only `rawInput`, so an edit's diff and paths are hidden from the user | independently confirmed | Cursor/Kimi approval details and display validation considered rawInput only. `src/server/provider/acp-permission-detail.ts` includes content and locations; tests cover missing rawInput and bidi controls in paths/patches. Existing rawInput-only display stays compatible. |
| [P6](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Medium · Security] Plan mode: Gemini refuses every permission request, Cursor and Kimi auto-approve them | independently confirmed | Client-side plan permission guards were absent. In-memory ACP tests verify both providers cancel edit requests even with full access. Kimi explicit AskUserQuestion interaction remains supported; this is a deliberate provider difference. No claim that current hosted CLIs actually request edits in plan mode. |
| [P7](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Medium · Performance] Every ACP frame is parsed twice, validated twice and deep-copied | deliberate tradeoff | Framing validates untrusted bytes before handing them to the SDK, whose object decoding validates at a second boundary. Deep redaction protects nested host-tool/provider secrets. Duplicate work is visible but no isolated throughput/RSS regression was measured. Casting SDK input or redacting only display fields would weaken guarantees; retained these boundaries. |
| [P8](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Medium · Structure] `ProviderManager` is a 1,100-line god class, and the Cursor harness is compressed to fit the line cap | deliberate tradeoff | ProviderManager and ACP lifecycle closures remain large. Dedicated run coordinator, installation, metadata and protocol helpers already separate some responsibilities. Member/line counts alone do not justify a full provider lifecycle rewrite in this repair; confirmed behavioral fixes stay focused. |
| [P9](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Low · Bug] The "identical" JSON-line transforms charge the event budget in different places, and a blank CRLF line kills the run | independently confirmed | acp-line-budget.test.ts failed seven cases before correction. Cursor/Kimi now charge before JSON parsing; all three adapters skip whitespace-only frames including split CRLF while charging each line. Invalid UTF-8/envelopes still fail. Combined ACP suites pass 114 tests. Native providers and full portable gate remain pending. |
| [P10](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Low · Bug] Smaller drifts between the copies | independently confirmed | Source confirms Cursor/Kimi graceful cancellation lacks Gemini promptInFlight guard and usage_update validation differs; Kimi still has separate replay flag. These smaller drift paths remain unresolved and need explicit pre-prompt cancellation/malformed usage regressions before changing adapters. |
| [P11](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Low · Quality] Dead state in Kimi: `suppressSessionProjection` | independently confirmed | Kimi suppressSessionProjection is set around load while sessionReady is false and the notification guard also requires sessionReady plus promptInFlight. Redundancy confirmed by control-flow inspection; no stronger replay guarantee was established. Removal is deferred with the adapter lifecycle audit. |
| [P12](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648804944) — [Low · Quality] Basic building blocks are reimplemented across modules | independently confirmed | Separate bounded framing/buffer/token helpers remain. ACP framing has new shared behavioral tests; overflow policies differ intentionally by protocol. Potential surrogate truncation needs a focused negative control before buffer consolidation; no blanket deduplication performed. |
| [M1](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [High · Performance] Every diagnostic record re-reads and JSON-parses all log files, then fsyncs, on the Electron main thread | independently confirmed | runtime-diagnostics.ensureDirectory invokes pruneExpired per record; append fsyncs synchronously in main. Source establishes repeated work, but UI delay and worst-case disk latency were not benchmarked. Directory revalidation/retention and durable failure receipts must survive any amortization; no unsafe one-time containment cache added. |
| [M2](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Bug] One transient write error makes detached chat windows impossible to close | independently confirmed | Draft-store #block permanently latches #writeBlocked even for transient-io, and #ensureWritableState rejects subsequent puts. Automatic retry remains unresolved: it must revalidate the pinned primary/backup after uncertain writes before resetting the latch. Empty draft removal is also a durable mutation and cannot simply be skipped. |
| [M3](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Bug] Private Connect `enable()` can leave the status stuck at `"starting"`, which blocks every app update | already fixed | Private Connect enable now constructs/stops the gateway, persists and starts within the same try/catch. Startup failures leave starting and emit failure state. Existing service failure coverage passed earlier in the 20-test service run; no new status workaround needed. |
| [M4](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Security] `openProjectPath` passes any project file to `shell.openPath`, including executables, and agent-authored markdown links reach it | independently confirmed | Privileged openProjectPath still calls shell.openPath after containment validation, so an explicit open action can launch an executable. Main transcript normally supplies onOpenProjectFile to the internal viewer, but fallback open links retain this behavior. Executable reveal/confirmation policy remains unresolved; no executable payload was launched. |
| [M5](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Security] Test hooks are gated on `process.env.NODE_ENV` at runtime in release builds, and v0.0.55 adds a fixture that turns off the Chromium sandbox | independently confirmed | Packaged main still has runtime NODE_ENV test gates and the Linux installed-update fixture adds no-sandbox when both test env and fixture root are supplied. This requires local launch environment control, and the exact installed-release upgrade proof depends on the fixture. Removal requires replacement artifact-identical certification; simply stripping it would break required tests. No release fuse was weakened. |
| [M6](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Performance] Every attachment-preview request forks a utility process and re-hashes the file, and the response is `no-store` | deliberate tradeoff | Preview access revalidates via a supervised utility and uses no-store. Metadata alone cannot prove unchanged hostile bytes (mtime can be restored); hash/containment checks protect attachment ownership. Process cost is plausible but unmeasured, and an authority-aware byte cache would require eviction/revocation proof. Retained validation. |
| [M7](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Performance] A new Electron `Session` is created for every preview slot and detached window, and never released (needs confirmation) | not reproduced | Random in-memory Electron partitions and storage clearing are present. Persistent session/RSS growth after repeated ownership changes has not been measured. Pooling partitions could mix cookies/handlers across owners; no pooling introduced without evidence and cleanup guarantees. |
| [M8](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Medium · Structure] `RuntimeSupervisor`'s quarantine/lock-out sequence is copied about 8 times with drifting fields, and test-only recycle logic is threaded through production code | deliberate tradeoff | Supervisor has repeated quarantine branches with distinct cleanup evidence and recovery eligibility. The release diagnostic currently investigates an Intel cleanup-join deadline, without an identified cause. No state-machine deduplication undertaken while exact cleanup behavior is unresolved. |
| [M9](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Quality] IPC channel names are hand-copied between preload and main, in three different styles | independently confirmed | Main/preload channel string duplication and differing shared constants are present. No actual mismatched channel was found in this review. A typed channel map is structural follow-up and must preserve the explicit sandboxed preload surface. |
| [M10](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Bug] `RuntimeSupervisor` honours its `platform` option only in some places | independently confirmed | Constructor stopAttempt uses options.platform but recoveryWaitMs and requiresExplicitModernDarwinRecovery use process.platform. This creates mixed injected-platform tests. Production default agrees; fix and cross-platform test updates remain pending. |
| [M11](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Structure/Performance] The AppImage install path is dead in production, duplicates staging, and hashes the full AppImage up to 5 times | deliberate tradeoff | Legacy AppImage helper refuses launching without verified bootstrap acknowledgement; runtime uses the prepared update protocol. Repeated hashes occur across separate identity/commit boundaries and cannot safely be carried over without mutation proof. Legacy helper cleanup is optional; no verification step removed. |
| [M12](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Bug] `compareSemver` uses locale-aware `localeCompare` instead of SemVer's ASCII ordering | independently confirmed | `src/main/app-update-handoff.ts:compareSemver` used localeCompare. ASCII Beta→alpha and reverse handoff fixtures failed before the change; ASCII comparisons now pass in `tests/main/app-update-handoff.test.ts` along with numeric prerelease cases. No release identity or version was changed. |
| [M13](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Performance] The Windows runtime broker polls stdout every 10 ms instead of reacting to `data` events | independently confirmed | Windows runtime broker uses bounded 10 ms polling and synchronous assembly validation. No native Windows CPU/latency measurements were taken locally. Replacing polling requires proving cancellation, lock loss and exact request correlation; current deadlines remain intact. |
| [M14](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Bug] The desktop `Notification` isn't retained, so its click handler may be lost to GC (needs confirmation) | not reproduced | Notification is locally allocated and shown; no native forced-GC click-loss reproduction has been performed. Source lifetime alone does not establish Electron native ownership behavior. No unbounded notification retention set introduced. |
| [M15](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805073) — [Low · Quality] Private Connect decides error codes by regex on the message text | independently confirmed | Private Connect catch maps sanitized message text containing uncertain to uncertain and otherwise forbidden. This couples wording and protocol/error recovery. Typed failure mapping is still unresolved; avoid exposing raw internal error types/details. |
| [R1](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [High · Performance] Every streamed token re-renders the whole app shell | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R2](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [High · Performance] `FilesPanel` schema-parses and serialises the whole previewed file on every render | independently confirmed | `src/renderer/src/components/FilesPanel.tsx` called canSaveFile inline. Editability is now memoized by immutable preview and callback identity. Actual token-rate cost and render timings were not measured. |
| [R3](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Bug] `TurnTimeline`'s memo comparator leaves out `providerIdentityLabels`, so settled turns keep stale labels | independently confirmed | `sameTurnTimelineProps` omitted providerIdentityLabels. The comparator now observes it; `tests/renderer/response-timeline-virtualization.test.ts` tests the settled-row invalidation alongside existing streaming memo controls. |
| [R4](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Bug] The Duo comparison poll re-shows the global error banner every ~750 ms, so it can't be dismissed | confirmed | Reproduced: unchanged cancellation status republishes the global error on the next 750 ms poll (negative control failed). Fixed by comparing the new message with the watched authoritative status before publishing. DOM regression verifies an unchanged poll stays quiet and the later completed cancellation updates recovery state; 13 comparison tests pass. |
| [R5](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Bug] After a successful send, attachment state and ref can diverge (needs confirmation) | independently confirmed | A deferred onSend plus requestComposerPrefill reproduced accepted attachment chips remaining with an empty unsent ref. Composer now removes only submitted attachment IDs after accepted ownership and disables chip removal while submission is pending, while preserving revised text. Composer ownership/lifecycle suites pass 86 tests; negative test failed before correction. |
| [R6](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Bug] The layout-estimate hook reports a false layout change on every conversation switch (needs confirmation) | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R7](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Performance] @mention search sends a filesystem query on every keystroke, while `FilesPanel` debounces the same command | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R8](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Quality] Duo `launchBlocked` is decided by substring-matching UI copy | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R9](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Quality] React purity lints are turned off while hot components write refs and mutate objects during render | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R10](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Medium · Structure] `Composer` and `App` are god components squeezed under the line cap | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R11](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Low · Performance] FilesPanel preview lines use inline `ref` callbacks, so every render re-binds up to 2,000 refs | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R12](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Low · Performance/Bug] `planSteps` is recomputed on every token and can come from the previous answer | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [R13](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805183) — [Low · Performance] The timeline's Alt-navigation keydown listener is removed and re-added on every timeline change | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T1](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [High · Quality] `server-event-schema.ts`, the runtime→client trust boundary, is the most compressed file in the repo, and the compression hides an inconsistency | independently confirmed | Safe revisions 2^31 and MAX_SAFE_INTEGER failed through parseServerEvent before the change. `src/shared/contracts/run-state-schema.ts` now uses Number.isSafeInteger; `tests/shared/server-event-schema.test.ts` also rejects fractional, infinite, NaN and unsafe revisions. The rest of the broad validator rewrite is not established by this boundary defect. |
| [T2](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Medium · Structure] Server events are defined in three places with no compile-time link, while client commands already use zod | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T3](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Medium · Performance/Tooling] Renderer-only libraries are production `dependencies`, so electron-builder packs them into `app.asar` | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T4](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Medium · Tests] 101 test cases are skipped on Windows despite AGENTS.md requiring explicit Windows coverage | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T5](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Medium · Tests] Real-time sleeps and wall-clock assertions are used as synchronisation (33 files) | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T6](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Medium · Tests/Performance] The suite is dominated by three git-fixture suites, and the Windows shards are 3.8× unbalanced with stale timing data | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T7](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Low · Bug] Journal renames have no retry for transient Windows sharing violations (needs confirmation) | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T8](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Low · Structure] `runtime-process-protocol.ts` is at the cap, mixes re-exports with validators, and mixes import specifier styles | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T9](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Low · Tests] Renderer coverage thresholds are very low for the largest part of the app | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T10](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Low · Tooling] `xlsx` is a production dependency installed from outside the npm registry | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [T11](https://github.com/eduardtomas1/inertia/issues/356#issuecomment-5648805275) — [Low · Tests] The test output is full of expected stderr noise | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |

## Issue #358

| Finding | Disposition | Evidence and limits |
| --- | --- | --- |
| [X4](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Structure] `src/server` has no internal layering: 7 of its directories form a single import cycle | deliberate tradeoff | Verified cross-directory imports in codecs.ts, turn-ledger-repository.ts and git/commit-review.ts. Directory aggregation is not a runtime module cycle: the current architecture gate has no file cycles, and independent usage-values/request-context/provider-activity-detail tests exist. The assertion that no server part can be tested independently is disproved by those suites. A domain-directory reorganization is optional; exact historical aggregate edge counts were not reproduced. |
| [X5](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Tooling] `check:architecture` claims to catch import cycles but ignores `import("…")` types, and it never checks external packages per layer | independently confirmed | moduleSyntax handles ImportDeclaration, TSImportEqualsDeclaration and dynamic import calls but omits TSImportType; resolveImport marks unknown bare specifiers external. desktop.ts/snapshots.ts contain reciprocal inline type imports. This is a static-analysis coverage gap, not a demonstrated runtime cycle. The cited vite.config.ts node:path import runs in scripts/build-private-connect.mjs as build configuration, not in the browser, so it does not demonstrate renderer filesystem authority. Expanded graph policy remains unimplemented. |
| [X6](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Quality] A third of all `catch` blocks discard the error, 278 more are `.catch(() => undefined)`, and only 2% log anything | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [X7](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Quality] 781 exports are never imported; 2 production files are dead, and one of them is still being edited | independently confirmed | Repository-wide filename/import search found no consumers of opencode-harness-values.ts or SidebarProjectState.tsx; both dead modules removed. The broader 781/512 export counts were not independently recomputed. A production-import ban already isolates cli-agent-harness.ts; moving that tested fixture or adding a dependency-wide dead-export gate remains structural work rather than a reproduced runtime defect. |
| [X8](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Structure] 52 functions exceed cyclomatic complexity 50, and each runtime command family is a single 580–940-line closure | deliberate tradeoff | turn-interaction-commands.ts retains a large family dispatch closure. The claim that an individual command cannot be tested independently is contradicted by direct message.send tests in turn-interaction-commands.test.ts, including 55 passing cases. Extraction may improve navigation but replacing the dispatcher and imposing a complexity ratchet is not needed for the independently reproduced title defect fixed here; original aggregate complexity figures were not reproduced. |
| [X9](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Structure] Two independent parsers of untrusted PNG/JPEG/GIF/WebP bytes in `src/main` | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [X10](https://github.com/eduardtomas1/inertia/issues/358) — [Medium · Docs] Two design documents state limits or inventories that the code no longer matches | independently confirmed | Compared document-extraction-scheduler.ts constants and response-timeline/changedFiles.tsx callbacks to both documents. DATA_THROUGHPUT now distinguishes the historical benchmark from the current 96 MiB reservation budget and explicitly states it is not a hard decoder/RSS bound. RENDERER_ISOLATION includes transcript changed-file callback authority. |
| [X11](https://github.com/eduardtomas1/inertia/issues/358) — [Low · Quality] Overall duplication is low (1.1%), but the clusters outside ACP copy code and let it drift, several across process layers | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [X12](https://github.com/eduardtomas1/inertia/issues/358) — [Low · Quality] The same limits are redefined as local magic constants in dozens of files | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [X13](https://github.com/eduardtomas1/inertia/issues/358) — [Low · Quality] There is no logger: 34 `console.*` calls in production code, some that drop the error, and one that only logs under `NODE_ENV=test` | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [X14](https://github.com/eduardtomas1/inertia/issues/358) — [Low · Dependencies] One production dependency is never imported, and three packages are imported but never declared | independently confirmed | package.json declares tailwind-merge but repository import search found no consumer. provider-drift-staging.mjs imports esbuild, windows-installer-smoke.mjs deep-imports app-builder-lib, and release-assets.test.ts imports @electron/asar without direct declarations. No dependency edits made yet: a change requires lockfile/notices/package verification, which remains pending. Claude SDK peer dependencies are not treated as unused. |
| [X15](https://github.com/eduardtomas1/inertia/issues/358) — [Low · Docs] `docs/` mixes point-in-time evidence reports and 42 MB of images with user docs; the CHANGELOG silently skips 0.0.38 | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [X16](https://github.com/eduardtomas1/inertia/issues/358) — [Low · Quality] Each layer has its own import-specifier and file-naming convention, and nothing enforces either | deliberate tradeoff | Source inspection confirms layer-dependent extension and naming conventions; tsconfig.node.json uses Bundler resolution, while main/node utility entry paths also participate in emitted ESM tooling. Mixed spelling and parser styles alone do not establish a runtime defect. No repository-wide rename/import rewrite or forced Zod dependency at privileged leaf boundaries was introduced; historical aggregate counts were not recomputed. |
| [N1](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [High · Build/Bug] The macOS guardian is built with no deployment target, so its minimum OS is whatever the CI runner runs, not the app's declared `13.0` | already fixed | build-runtime-process-guardian.mjs explicitly passes -mmacosx-version-min using the configured macOS deployment target, and the current build/guardian contract suite passes locally. This is inherited from merged #357, not a new fix on this branch. A macOS 13 machine was not used here. |
| [N2](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [High · Bug] Linux drain gives up **without killing anything** once the child census fails permanently, for example with more than 256 direct children | independently confirmed | Inspected linux.c census/drain: MAX_CHILDREN=256, overflow closes acquired pidfds and returns failure, and several freeze failures return without resuming/killing held children. The guardian reports unproved cleanup rather than success, but descendants can remain. No process swarm was run. A correct bounded batching/cleanup design and Linux native proof remain unresolved; raising the cap is not a complete fix. |
| [N3](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [High · Bug] The Linux guardian's `NO_NEW_PRIVS` and hard `RLIMIT_CORE=0` leak into every payload, including the user's interactive terminals, which breaks `sudo` | deliberate tradeoff | linux.c sets no_new_privs before payload fork and hard RLIMIT_CORE=0 in main, so children inherit restrictions. These are existing containment choices, not v55 regressions. Moving them past fork would expand provider/terminal privilege and invalidate present complete-tree assumptions. No sudo/elevated child was launched and no restriction removed. |
| [N4](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Medium · Performance] macOS: after the first fork, each guardian re-scans **every process on the machine** every 240 ms | not reproduced | darwin.c confirms PROC_ALL_PIDS census after fork taint at up to 240 ms intervals. The claimed 80k syscalls/second was not measured. Filtering by UID can hide descendants that change identity and cannot replace complete-tree evidence without lifecycle proof. No event-census redesign was introduced. |
| [N5](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Medium · Security/Structure] Windows containment depends on `powershell -ExecutionPolicy Bypass -EncodedCommand` + in-memory `Assembly.Load` | externally blocked | The Windows broker/update loader source uses PowerShell and Reflection.Assembly.Load. No WDAC/AppLocker/EDR-controlled Windows environment is available for the claimed policy failure. Replacing the verified locked-image launch path requires native authority/cleanup equivalence tests, not just removing the script wrapper. No policies or endpoint controls changed. |
| [N6](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Bug] The Linux payload child keeps the guardian's signal handlers between fork and exec; `darwin.c` explicitly fixes this | independently confirmed | linux.c gate-blocked payload branch inherits handlers and retries read on EINTR, unlike Darwin reset behavior; all exec failures currently map to 127. No precise gate-window signal witness was run. Correcting it requires blocking signals across fork/reset and native pre-admission cancellation proof; this boundary remains unchanged. |
| [N7](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Bug] `LaunchUpdateSupervisor` can misreport an admitted supervisor as failed | independently confirmed | LaunchUpdateSupervisor stopped waiting when HasExited before the READY reader published. It now waits for the bounded ManualResetEvent publication and only reads shared results after a successful wait, covering immediate EOF and establishing synchronization. Local Windows contract suites pass; actual C# execution/race proof is still pending Windows CI. |
| [N8](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Security/Quality] The Linux "guardian executable identity" describes the probe helper, not the target process | independently confirmed | linux.c identity_mode stats /proc/self/exe while reporting another PID; that metadata is the reporting helper identity, not proof of the target executable inode. PID/start/parent/state checks still constrain authority. No spoofed-target unauthorized signal was reproduced. Changing target /proc access must account for dumpable=0 and pidfd lifetime; unresolved. |
| [N9](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Security/Quality] Linux claim/authorize trusts a single overwritable `si_pid` slot, and `trusted_runtime_helper` opens a pidfd it never uses | not reproduced | claim_handler/authorize_handler store a single sender PID for ordinary signals, so coalescing is a protocol risk. No concurrent legitimate claim or hostile-signal witness was run. Same-UID denial of service is broader than this signal channel; queued signals/socket authentication would be a protocol redesign requiring portable/native fixtures. |
| [N10](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Security/Bug] Windows `GuardOwned` checks the parent before opening the handle, leaving a PID-reuse window | independently confirmed | GuardOwned checked parent identity before acquiring the process handle. It now opens and retains process.Handle before ExpectedParent and creation-time validation, matching the other guarded paths. Existing guardian/Windows contracts pass locally; PID-reuse and actual Windows Job execution remain pending native CI. |
| [N11](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Build] No explicit hardening flags, and the musl build can silently fall back to glibc headers | not reproduced | Build script lacks the proposed explicit linker/stack flags and uses idirafter kernel-header fallbacks. This establishes reliance on toolchain defaults, not an observed vulnerable binary or mixed-libc ABI fault. The current Linux artifact was not analyzed with readelf here. A musl/UAPI/toolchain hardening change needs package tests and is not inferred from absent flags alone. |
| [N12](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Build] The Windows helper is built with legacy .NET Framework `csc`: not reproducible, not signed | deliberate tradeoff | Build script uses the trusted installed Framework csc.exe with anycpu and no deterministic flag. Reproducible-byte identity was not tested with two Windows builds. Artifact hashes still bind verified bytes and are not equivalent to reproducible builds or signing. Changing compiler/runtime and enabling Authenticode requires separately configured tooling/credentials; neither was changed. |
| [N13](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Performance] Synchronous `spawnSync` guardian helpers block the Electron main thread during crash recovery | independently confirmed | Recovery and journal helpers include synchronous bounded native identity/session probes. Repeated records can block startup while proving ownership; no multi-record UI freeze timing was measured. An async migration must preserve aggregate deadlines, recovery ordering and exact cleanup evidence; current bounded path retained. |
| [N14](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648969994) — [Low · Structure] Duplication, divergent twin implementations, and test hooks inside production control flow | deliberate tradeoff | Native files contain duplicated protocol/state handling and compile-time test macros. Their call-site authority and terminal-result meanings differ; identical-looking paths cannot be merged solely by a duplication count. Compile-time test variants are not user-controlled production environment bypasses. No concrete behavioral failure was reproduced for this aggregate structural claim. |
| [B1](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [High · Security] Anyone with write access, including an agent branch, can define and run the release pipeline: no environment, no ref guard, no tag ruleset | externally blocked | Read-only repository settings currently return zero rulesets and environments, Actions allowed_actions=all and sha_pinning_required=false. Workflow dispatch and contents-write publication require repository policy decisions. No ACL, environment, signing-secret, tag or main change is authorized here; settings were not modified. |
| [B2](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [High · Security] Release tags are mutable and nothing ties them to a reviewed `main` commit; the lineage tamper check never runs at release time | independently confirmed | Release source freezes exact tag/commit and rechecks remote identity, but lacks a main-ancestry/merge-ready admission requirement. Repository ruleset changes require external configuration. Read-only GitHub API on 2026-09-13 confirms draft 385170089 is unpublished (published_at null), empty, targets 5c9dc929; annotated tag 62e5894c peels to the same commit. The issue historical eed32810/orphaned-draft state is stale; reconciling this unpublished candidate is not evidence that a published release was rewritten. No tags, releases or repository ACLs changed here. |
| [B3](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [High · Security] Update and installer authenticity comes only from the same GitHub release; provenance is generated but never verified; Linux asar integrity is a no-op | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [B4](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Bug] The stable feed can be rolled back: publishing forces `--latest` with no version check, unlike Canary | confirmed | Stable publication still unconditionally uses --latest, while workflow concurrency is scoped to each tag. Older independent stable releases can therefore replace latest. A global serialized monotonic publisher is still required; no release was published or edited during this audit. |
| [B5](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Security] Signing credentials share a 60–100 min job with third-party code; the signed path has never run and uses weak default entitlements | confirmed | Fixed key lifetime: the macOS packaging step now decodes under umask 077, exports only inside that shell, unsets the base64 input, and removes its key with EXIT plus an always cleanup step. Unit tests/build precede key creation, contrary to the claimed whole-job exposure; later desktop tests previously followed it. Separate signing authority, narrowed tested entitlements, signer pinning and actual signed-path exercise remain unresolved/external; no secrets were read. |
| [B6](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Security] The provider-drift canary runs install scripts from unvetted `@latest` CLIs and an unpinned Cursor binary, in a job that shares the default-branch Actions cache (needs confirmation) | confirmed | Removed setup-node npm caching from the latest-provider probe job to avoid shared cache restoration/post-save. Existing probe environment allowlist, isolated HOME/config, read-only job token and separate issues-write reporter reduce exposure. Latest CLI code still executes as the same runner user: environment filtering is not a security sandbox, and full isolation is unresolved. Claimed node_modules-cache reuse is not present in this workflow (it uses npm ci directly). |
| [B7](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Quality] CI never builds the Linux *release* configuration or runs the release staging and publish code | confirmed | platforms.json still selects base Linux package scripts while Windows/macOS use release scripts. Release metadata and stage/finalize/publisher behavior therefore have different CI coverage. Changing this safely requires Linux artifact/config verification; no packaging behavior was changed yet. |
| [B8](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Performance] The release critical path re-runs the full unit suite serially inside each packaging job, and v0.0.55 timed out because of it | confirmed | Release Windows jobs still run the complete suite serially, while ordinary CI has four Windows shards. Historical runner timings were not remeasured locally. Retaining release certification is necessary until exact-SHA evidence reuse or separately gated release shards are implemented; no timeout or gate was weakened. |
| [B9](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Performance] Every merge is fully certified twice, with 12 full-history clones per run | deliberate tradeoff | Full post-merge certification and broad workflow classification are conservative release gates. Strict branch freshness alone does not prove equivalent exact-commit contexts or artifacts. Verified tree/evidence reuse would need an explicit trusted receipt; no gates or fetch history requirements were removed based solely on historic cost estimates. |
| [B10](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Medium · Build] The compiler is only `strict`, and 16.8k lines of release-critical `.mjs` aren't type-checked at all | confirmed | Strict TypeScript is enabled but the additional named flags and script checkJs are absent. This is a static-analysis coverage gap, not evidence that every reported diagnostic is a runtime defect. No bulk compiler-policy migration or unverified shim removal was attempted; focused behavioral tests cover the new bundle parser. |
| [B11](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Low · Structure] Release and CI YAML are copy-pasted and already drifting; the dev packaging scripts are dead | deliberate tradeoff | CI and release retain duplicate platform proof blocks; duplication can drift but supports distinct package admission paths. Developer packaging commands remain usable public scripts even without internal callers. Composite extraction/deleting public commands is not justified by reference counts alone; checks remain intact. |
| [B12](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Low · Bug] The renderer bundle-budget import closure only recognises two import forms (needs confirmation) | confirmed | Fixed: emitted-module AST parsing counts default, namespace, named, side-effect and re-export dependencies while excluding dynamic imports and decoy strings/comments. Three focused parser tests pass, malformed modules fail closed, and the current bundle passes unchanged byte ceilings. Uses the existing pinned @babel/parser dependency. |
| [B13](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970105) — [Low · Security/Quality] Workflow hygiene | confirmed | Fixed direct shell expression interpolation by using existing RELEASE_TAG env and restricted both reporters to matching marker plus github-actions[bot] author. Minimum-Node npm cache caches downloads, not node_modules or test results: npm ci still verifies/reinstalls dependencies. Apt pinning and dependency cooldown policy remain unchanged; neither demonstrates an observed bypass by itself. Workflow/environment focused tests pass (18, one platform skip). |
| [PC1](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Medium · Security] The session cookie isn't isolated by port, so every other HTTPS service on the same MagicDNS host receives the full bearer credential | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC2](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Medium · Security] The Tailscale Serve mapping is left published after quit, forwarding tailnet traffic to a loopback port that any process can bind later | independently confirmed | `PrivateConnectService.shutdown` stopped only the gateway. It now removes only the mapping authorized by its current port/persisted proof, with a deadline and gateway cleanup in finally. Restart test now asserts removal; real Tailscale service behavior was not exercised. |
| [PC3](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Medium · Security/Bug] "Locking the desktop closes every connection" is false on Linux, and a suspend with no unlock event never resumes | independently confirmed | PrivacyMonitor subscribed to suspend without resume. Resume now samples idle state and opens only for active/idle. Tests cover locked/unknown and listener shutdown. Linux lock-event support remains an explicitly documented platform limitation; no Linux lock event was manufactured. |
| [PC4](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Medium · Security] "Transcript text strips credentials" is regex best-effort and misses most real key formats | independently confirmed | Synthetic AWS, Google, GitLab, npm, password assignment and OpenSSH formats exercise the sanitizer. Known formats are now redacted and docs say best-effort; arbitrary unknown secret detection is not claimed. Sanitizers with different output policies were not globally merged. |
| [PC5](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Medium · Security] Remote mutations are never audited, and connect churn evicts the security history | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC6](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Security] One device can take every WebSocket slot, and the socket rate limit is per socket, not per device as documented | independently confirmed | The gateway kept requestTimes per socket and only a global socket cap. It now admits two sockets per device and shares the HTTP/WebSocket request bucket. Local loopback integration tests cover two sockets sharing quota, third rejection and another device continuing. |
| [PC7](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Security] Any tailnet peer can starve pairing and fingerprint the host without credentials | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC8](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Security] The runtime doesn't enforce input or stop scopes; the main process is the only gate | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC9](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Bug] Any connected browser, even a read-only Monitor tab, blocks app updates with no override | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC10](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Bug] After a failed authority change, re-enabling orphans the running gateway and reports "ready" while still fail-closed | independently confirmed | enable did not reject pendingAuthorityReduction or stop a still-running previous gateway. It now refuses pending authority changes and stops an active old gateway before replacing it. Existing startup failure tests continue to require exact admitted-port cleanup. Dedicated failed-authority retry test remains pending. |
| [PC11](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Security] Smaller docs-vs-code gaps: the pairing secret isn't compared in constant time, the invitation isn't consumed at start, and the WebSocket accepts plain-http loopback origins | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC12](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Quality] Duplicated contracts and dead code between the web client and the shared modules | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC13](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Low · Tests] The security tests miss the risky behaviours, and one test locks in an unsafe one | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [PC14](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970223) — [Medium · Security/Performance] The Private Connect sanitizer itself has a quadratic regex, run for up to 200 messages per request | independently confirmed | The credential-URL and trailing-fragment expressions retried unbounded token suffixes. They are replaced by monotonic/token scans in credential-redaction.ts. An isolated 64 MiB Node child checks 800k-character inputs with 4-second time and 4 KiB output bounds; ordinary content and long-scheme redaction tests remain. |
| [W1](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [High · Security/Bug] A PDF of a few hundred bytes with a compressed content stream crashes the shared runtime on memory | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W2](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [High · Security/DoS] The regex that neutralises untrusted agent text is quadratic and runs *before* the byte cap | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W3](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [High · Bug] Once `inertia.sqlite` passes 256 MiB, every Linux and Windows update is rejected as "database-incompatible" | independently confirmed | app-update-candidate-viability-worker.ts isolatedDatabaseClone explicitly rejects page_size × page_count above 256 MiB and constrains migration growth. Automated candidate validation can reject healthy large profiles. Manual install failure is not established. No cap increase or unsafe temporary profile copy was introduced; WAL-inclusive disk snapshot confidentiality, identity, cleanup and disk bounds remain unresolved. |
| [W4](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Medium · Bug] Workspace listing and search fail entirely when one entry disappears mid-scan | independently confirmed | New `tests/server/workspace.test.ts` witnesses delete an entry via afterEntryObserved. Both listing and search failed on baseline and pass after skipping only ENOENT/ENOTDIR. Existing parent-replacement and external-link negative controls remain. |
| [W5](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Medium · Bug (Windows)] Windows command lookup returns the extensionless POSIX shim before the `.cmd`/`.exe` | independently confirmed | environment.ts placed bare Windows commands before PATHEXT siblings. An isolated shim fixture failed before correction; lookup now uses ordered PATHEXT candidates and accepts an injected platform. Explicit extensions and POSIX executable handling remain tested. Environment/Windows discovery: 32 passed, four native-platform cases skipped on macOS. Native Windows launch remains unexercised. |
| [W6](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Medium · Bug] Workspace path authority: legacy projects that fail enrolment are never retried, and filesystems without a creation time can't enrol (the filesystem part needs confirmation) | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W7](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Medium · Security] Agent-browser approval prompts show internal refs and hide the text being typed | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W8](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Medium · Bug] An isolated review or compaction slot is never released when `stopOwned` returns `"missing"` (needs confirmation) | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W9](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Medium · Performance] Each selective-reversal `prepare()` can start about 250 git processes at once | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W10](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Low · Bug] Any stray file in the generated-attachments folder stops the runtime from starting | independently confirmed | PrivateGeneratedAttachmentStore.create rejected all non-UUID JPEG names. It now preserves regular .DS_Store/desktop.ini/Thumbs.db without inventorying them as generated content. Unknown files and unsafe metadata entries remain rejected, preserving foreign data rather than silently deleting/quarantining it. Seven store tests pass; startup through a packaged app remains pending. |
| [W11](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Low · Bug] Lookup tables built from plain objects answer `constructor` and `__proto__` | independently confirmed | Inherited constructor/__proto__ lookup tests failed on baseline. `attachments.ts` and `source-language.ts` now require own keys; extension, basename, alias and MIME admission tests cover inherited keys and existing valid lookups. |
| [W12](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Low · Bug (Windows)] Windows path checks disagree with each other, and the reserved-name list is incomplete | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [W13](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970285) — [Low · Structure] Security-relevant primitives are copy-pasted across modules, and the copies have started to drift | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U1](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [High · Bug/Structure] A failed lazy chunk blanks the whole window, and the failure is cached for the rest of the session | independently confirmed | `useLoadedSurface` throws failures during render; main.tsx had no ancestor boundary. A root WindowErrorBoundary now presents reload; createSurfaceLoader evicts rejections and handles unawaited prefetch rejection. DOM tests exercise actual hook rejection and retry caching. Native missing-chunk incident and per-surface state-preserving retry are not claimed. |
| [U2](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [High · Bug] Review "Note" and "Edit note" call `window.prompt`, which Electron doesn't implement | independently confirmed | ChangesPanel called window.prompt for file/hunk creation and editing. It now uses ReviewNoteDialog with focus restoration, bounded text, cancellation and retry without losing draft text. DOM dialog tests pass; panel wiring and native visual proof remain pending. |
| [U3](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [High · Accessibility] The "Large" interface scale leaves 170 hard-coded sub-10px font sizes unscaled | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U4](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [High · Bug/Accessibility] Global shortcuts use Ctrl on macOS and steal Ctrl+B/J/K/N from the terminal and text fields | independently confirmed | `installGlobalShortcuts` captured Meta or Control on all platforms. It now distinguishes platform modifiers, uses physical letter codes, and preserves terminal Control chords. Unit/DOM tests cover Mac Control, Command on a non-Latin layout, and terminal K/J/B/N. Native xterm proof remains pending. |
| [U5](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Medium · Accessibility] The model chooser's `aria-activedescendant` points at plain buttons, so arrow-key navigation is silent to screen readers | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U6](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Medium · Accessibility] Terminal tabs are `role="tab"` with no arrow-key support, and the tablist contains non-tab children | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U7](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Medium · Accessibility/Quality] Escape and outside-click dismissal is hand-rolled in 7 places with different behaviour; one dialog has no Escape at all | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U8](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Medium · Bug] Startup and split-view `localStorage` access is unguarded, unlike the rest of the codebase | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U9](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Medium · Quality] Dead code: an orphaned sidebar component, 37 dead CSS classes, and a test suite covering a hook production never calls | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U10](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Medium · Structure] The CSS cascade depends on import order: 493 selectors are defined more than once, 184 with conflicting values | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U11](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Low · Bug (i18n)] Dates and times bypass the fixed `"en"` interface locale, and two plurals read "1 branches" | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U12](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Low · Quality] Duplicated helpers, two of which bypass the privileged clipboard bridge | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U13](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Low · Quality] Theme-token drift: hard-coded colours bypass the theme, and the bootstrap duplicates the theme list | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U14](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Low · Performance] A full-height `backdrop-filter` on the sidebar, and whole-document restyles on hide/show | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [U15](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970363) — [Low · Quality] `ConversationContextDialog` uses minified class names in source | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q1](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [High · Tests] Git fixtures inherit the developer's `~/.gitconfig`: one global hook or `commit.gpgsign` breaks most git suites | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q2](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [High · Tests] 100 test files assert on source text, so much of the renderer "behaviour" layer is really change detection | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q3](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Tooling] Security coverage thresholds are measured over whole globs, not per file, so weak files hide behind strong ones | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q4](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Tests] The checkpoint ref guard, the only barrier before `update-ref -d` and `git restore -- .`, has no negative test | independently confirmed | Added real-Git negative cases for restoreCheckpoint and deleteCheckpoint: foreign conversation, main branch, traversal, uppercase and suffix. Each verifies worktree bytes, real index, checkpoint ref and main ref survive. This confirms missing coverage, not a reproduced reference-guard bypass. |
| [Q5](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Tests] Benchmarks gate nothing: two are never run, and the enforced one only catches ~100× regressions | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q6](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Tooling] Nothing stops a committed `test.only` from reducing the E2E run to one test | independently confirmed | Both Playwright configurations lacked forbidOnly. They now set forbidOnly: Boolean(process.env.CI), preventing focused tests from silently reducing CI execution. Native runs remain pending. |
| [Q7](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Tests] E2E time limits grow with the worker count, and the shared fixture has a hidden 250 ms sleep called 147 times | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q8](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Structure] Helpers are re-implemented file by file across three helper roots, and the copied `git()` helper is what produced Q1 | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q9](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Medium · Structure] Test files are squeezed to fit the 2,500-line cap, and the desktop benchmark spec avoids the E2E cap entirely | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q10](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Low · Tests] E2E specs lean on CSS class selectors and main-process test backdoors | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q11](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Low · Structure] Four `*.dom.test.ts` files don't run in Happy DOM | pending | Independent review pending. The issue location is a claim, not reproduction evidence. |
| [Q12](https://github.com/eduardtomas1/inertia/issues/358#issuecomment-5648970479) — [Low · Tests] Hostile-markdown tests use `toBeTruthy()` where they should assert the security property | independently confirmed | Strengthened malformed-input assertion to reject javascript anchors and assert literal fenced content; replaced blocked-query truthiness with DOM presence assertions. All 21 hostile-markdown cases pass. Baseline already separately asserted javascript links become blocked spans and privileged-protocol anchors are absent; the broad claim of no security assertions was inaccurate. |

